pipeline {
    agent any

    stages {
        stage('Initialize') {
            steps {
                echo "Jenkins setup verification"
            }
        }

        stage('Build') {
            steps {
                echo "Building application..."
            }
        }

        stage('Deploy') {
            steps {
                echo "Deployment step simulated"
            }
        }
    }
}
